import React from "react"

const Title = ({ children }) => (
  <h1
    style={{
      textAlign: "center",
      margin: "5px",
      padding: "10px"
    }}
  >
    {children}
  </h1>
)

export default Title
